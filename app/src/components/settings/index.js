import React, { Fragment } from 'react';
import ReactDOM from 'react-dom';
import './style.scss';

const Settings = () => (
	<Fragment>
		<div>Страница настроек</div>
	</Fragment>
);

export { Settings }